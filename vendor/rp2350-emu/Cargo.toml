# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88"
name = "rp2350-emu"
version = "0.2.5"
build = false
exclude = [
    "tests/hello_dma.rs",
    "tests/hello_pwm.rs",
    "tests/hello_riscv_blinky.rs",
    "tests/hello_timer.rs",
    "tests/hello_uart.rs",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Cycle-accurate emulator for the Raspberry Pi RP2350 / RP2354 (dual Cortex-M33 + PIO + FPU)."
homepage = "https://github.com/0x4D44/picoem"
documentation = "https://docs.rs/rp2350-emu"
readme = "README.md"
keywords = [
    "emulator",
    "rp2350",
    "rp2354",
    "cortex-m33",
    "pico",
]
categories = [
    "emulators",
    "embedded",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/0x4D44/picoem"
resolver = "2"

[package.metadata.docs.rs]
features = [
    "threading",
    "testing",
    "test-hooks",
]
targets = ["x86_64-unknown-linux-gnu"]
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
default = []
test-hooks = ["picoem-common/test-hooks"]
testing = []
threading = []

[lib]
name = "rp2350_emu"
path = "src/lib.rs"

[[test]]
name = "dma_quantum_invariance"
path = "tests/dma_quantum_invariance.rs"

[[test]]
name = "dual_model"
path = "tests/dual_model.rs"
required-features = ["threading"]

[[test]]
name = "execution_model"
path = "tests/execution_model.rs"
required-features = ["testing"]

[[test]]
name = "lazy_fp"
path = "tests/lazy_fp.rs"

[[test]]
name = "threaded_bus_paths"
path = "tests/threaded_bus_paths.rs"
required-features = ["threading"]

[dependencies.picoem-common]
version = "0.2"

[dependencies.sha2]
version = "0.10"
features = ["compress"]

[dependencies.tracing]
version = "0.1"

[dev-dependencies.tracing-subscriber]
version = "0.3"
features = ["env-filter"]

[lints.clippy]
enum_variant_names = "allow"
large_enum_variant = "allow"
needless_range_loop = "allow"
too_many_arguments = "allow"
type_complexity = "allow"
unusual_byte_groupings = "allow"
