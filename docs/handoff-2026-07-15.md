# Session handoff — 2026-07-15 ~3am

Written at end of a long session (user asleep; may not have run
/compact). This is the context a fresh session needs. Master is clean
at the commit carrying this doc; every agent worktree is merged and
removed; nothing is scheduled to ping.

## 1. The overnight run (check FIRST)

Unit `pio-l3-monsters`, detached via systemd-run (--collect, 24h cap,
MemoryMax=48G, MemorySwapMax=0), launched ~3am running BOTH remaining
L=3 monsters sequentially:

```
cd /home/ben/Documents/programmingSync/pio_optimization/pio_superopt
./target/release/superopt narrow-split --spec tx-a --len 3 \
  --wrap-lo 1 --wrap-hi 2 --threads 28 --memo-cap 2097152 \
  --trace /data/pio_optimization/runs/narrow-split-l3-w12.jsonl
# then the same with --wrap-lo 0 --wrap-hi 2, trace ...-w02.jsonl
```

Log: `/data/pio_optimization/runs/l3_monsters.log` (check mtime for
liveness, not systemctl). **If dead/incomplete: rerun the exact same
command(s) — unit-level resume skips settled units.** Rules: same
binary (do NOT rebuild after new commits — the trace header pins the
engine git rev and refuses a mismatched resume), same --threads (or
pin --target 3584), never write partial analysis into the trace dir.
Traces are ~1GB each (~630B/unit, ~1.68M units); they contain per-unit
telemetry (verdict, items, wall, memo stats) + periodic aggregate
snapshots (fork-kind attribution, memo counters) — this IS the
datamining payload; mine the tail distribution for the recursive-unit-
split driver decision.

Context for expectations: 1..2 previously reached 60.5% settled in ~7h
on the pre-tags/pre-relaxation engine (that run: ~235B worker items,
not resumable, lost). Tonight's engine is ~10% fewer items and has the
full memo recovered (+42% hits at equal wall). 0..2 has never run.
If BOTH refute: footprint ≤3 is impossible for tx_a; L=4 rediscovery
ladder unlocks.

## 2. What landed tonight (all merged, all gated — see git log)

In order: overnight-verdict banking (2..2 REFUTED 132.0B/2h55m —
ladder 4/6); ticket 012 design (outcome-predicate reads / value-set
constraints; composition ruling: 011+012 are one lattice, 011 re-cut
to stop at (b)); tiny-champion eyeball battery + doc (CL1-CL6
candidate lemmas — read docs/analysis/tiny-champion-eyeball.md, it's
short and was written to be read); SMT equiv() driver (Layer 1 of the
proof engine; 2,144 word_canon pairs proven universal, CL1/CL2 proven
as the first CONDITIONAL rules, zero mirror divergences; limit =
supported_config, not opcodes); S2 relaxation review doc
(docs/analysis/s2-relaxation-review.md) which found S7; runner
narrow-split subcommand (unit-resumable); 011(a)+(b) implementation
(x/y tags, −10.3% L=3 items wall-neutral); S7 fix + S2 relaxation +
012 stage-0 census (first census table in the agent report / journal).

USER RULINGS tonight (do not re-litigate): no un-resumable long runs;
S2 relaxation approved after review; 011/012 one-lattice composition
approved; proof-engine track approved with equiv-first sequencing;
time-gating relaxed for resumable runs.

## 3. Open items, priority order

1. **Bank the monster verdicts** (STATUS/journal/memory) + mine the
   per-unit tail from the traces.
2. **T1 follow-up (soundness, open):** the 011(b) agent could NOT
   construct a red rig for tag-blind `project_state` (garbage
   projection); the tag-aware projection shipped as documented
   invariant / defense in depth. Its analysis sketches a possibly-
   unsound trajectory (prober skips the SET via jmp, re-syncs via
   WAIT, reads x around a loop re-executing the SET) that kept
   colliding with capture parity — worth one more adversarial attempt.
3. **012 stage 1** (JMP zero-test predicates): design in ticket 012
   §6; stage-0 census says 2-way predicate kinds are 5.4% of collapses
   at 16x width reduction (2..2, 150s). Note ctr-thresh/out-pinvis
   have NO collapse-site surface until OSR tags exist (stage 4).
4. **Mirror config-coverage extension** for equiv(): 68.7K battery-only
   pairs are blocked on supported_config (tx_a itself!), only 17.5K on
   out-of-subset opcodes. Then Layer 2 (rule library as data with
   licenses + stored proof queries).
5. **008 §3b re-measurement trigger FIRED** (user directive: re-test
   walk economics after any evaluator-adjacent cost change; tonight's
   tag branches qualify). Idle-box, revert 95b1b32 to test.
6. Sequential instrumented 1..2 slice (pred census + near-miss probe
   on a real monster wall — split workers are uninstrumented by
   design, so tonight's run does not produce these). ≤4 impossibility
   re-proof via equiv (stale post-fidelity caveat). Extend
   assert_champions_sound widening to legacy tests (cheap follow-up
   noted by the 011 agent).

## 4. Warnings / gotchas

- narrow_soundness suite now takes ~5 min (S7 canary). If that
  friction hurts, consider #[ignore] + CI-only — but it is currently
  the only red-green gate on the S7 class.
- `--ignored tx_a_l3_22_mine` substring-matches the smallmemo test —
  filter exact names when mining.
- The runner's tx_a fixture is duplicated in src/fixtures.rs (tests
  untouched); the byte-identical cross-check vs the test path is the
  drift alarm.
- Stats/Champion now derive serde; project_state is Vec<u64> with
  structural tag encoding; Item = 272B (budget test tracks it).
- Verdict-gate reference numbers on THIS tree: L=2 0..1
  45,201,271 items / 12,614 hits; L=2 1..1 33,653,383 / 10,854;
  L=3 0..1 700,564,042 / 21,280,552 / 29s.
